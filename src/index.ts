// You should probably not uncomment any of the commented imports
// as they are only used when generating new Meta Files
// (which probably isn't what you want to do)

// Filtering (removing corrupted images and extracting only square ones)
// import './filter.js'

// Meta File Generation
// import './concatenate.js'

// Analyzing (details on how to parse Meta Files included!)
import './analyze.js'
